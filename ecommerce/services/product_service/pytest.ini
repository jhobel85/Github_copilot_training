[pytest]
# This service is its own independent package. Anchor pytest here so a run
# started from this directory does not inherit the repository-root pytest.ini
# (whose testpaths target the root tests/ dir) and so service suites are never
# collected together with other services, which share module names (app, tests).
